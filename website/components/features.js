import React from 'react';
export default () => (
  <div>
    <p className="text-lg mb-2 text-gray-600 md:text-xl">
      After.js enables Next.js-like data fetching with any React SSR app that
      uses React Router.
    </p>
  </div>
);
