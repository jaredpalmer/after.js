{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["lib/**", "dist/**", "build/**"]
    },
    "lint": {},
    "test:watch": {
      "cache": false
    }
  }
}
